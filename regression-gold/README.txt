files in this folder represent our regression gold files
these should be commited to git, our regression tests will always compare themselves
against these files, these files can be regenerated anytime you make a breaking change 
to the model using `npm run regenerate-gold` and then commiting the result